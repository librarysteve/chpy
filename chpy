who=$(whoami)

if [[ who == 'root' ]]; then
  update-alternatives --config python || echo "Failed! did you follow the instructions?"
else
  echo "Woh! Hold on there friend, you need permission for that!"
  echo "Try sudo chpy"
